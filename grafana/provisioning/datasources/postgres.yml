apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: datawarehouse
    user: datauser
    secureJsonData:
      password: datapass123
    jsonData:
      sslmode: disable
      postgresVersion: 1500
    isDefault: true
    editable: true
